node_modules
.next
out
dist
.env
.env*.local
images/
.DS_Store
*.log
.vercel
tsconfig.tsbuildinfo

# Working copies of the source images. The Desktop originals stay with the
# owner; only the compressed WebP the site actually serves is committed.
public/**/*.png
!public/aly-logo.png
