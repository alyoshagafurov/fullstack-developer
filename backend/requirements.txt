# Pinned deliberately. The Next.js side pins Prisma 6.19.3 for the same
# reason: a surprise major bump in a lead-storage path is not an acceptable
# way to find out about breaking changes.
django==6.1
djangorestframework==3.18.0
psycopg[binary]==3.3.4

# Production serving. Both are deployment machinery, not application code:
# gunicorn runs the WSGI app, whitenoise serves Django Admin's own static
# files so the emergency backoffice is usable without a second web server.
gunicorn==23.0.0
whitenoise==6.11.0
